#########################################
# prompt/modules/ssh_hostname
# Author: Rui Pinheiro
#
# Zsh prompt "SSH Hostname" module

function mkprompt_ssh_hostname {
	if is_ssh ; then
		mkprompt_add "$fg[yellow]" "%m"
	fi
}
